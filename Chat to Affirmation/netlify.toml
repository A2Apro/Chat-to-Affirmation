[build]
  base = "Chat to Affirmation"
  publish = "."
  command = "npm install && npm run build"
  ignore = "false"  
  # ^ THIS IS THE MAGIC KEY. It forces Netlify to never skip.

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
